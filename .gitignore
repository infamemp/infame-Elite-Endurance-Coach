# Archivos del sistema operativo (Basura que crean Windows y Mac)
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
.AppleDouble
.LSOverride

# Respaldos o archivos temporales de editores de texto
*.tmp
*.bak
*.swp
~*

# Carpetas privadas (Por si en el futuro guardas datos sensibles de atletas)
privado/
datos_personales/

data/
*.xlsx
out/

config/athletes/*.yaml
!config/athletes/_template.yaml
!config/athletes/TESTRAMP.yaml

__pycache__/

athlete_docs/

# Local virtualenvs for the optional mcp_server/ dependency — never committed.
.venv*/

# mcp_server's own crash/debug log, written by run_server.py — local
# diagnostics only, may contain data/profile.md content in a traceback.
mcp_server/logs/